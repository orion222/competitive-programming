
user = input().split()
user = list(user)

matrix = [
    [1, 2, 3]
    # user input actually goes here
]

matrix.append(user)

print(matrix)
# console will print matrix with no formation
print(matrix[1][2])
# but, it is virtually in its structure as this indexing works
